const cloudinary = require("../middleware/cloudinary");
const mongoose = require("mongoose");
const MessageGroup = require("../models/Like")

module.exports = {
    //send message to user
    sendMessage: async (req, res) => {

    }
  
};
